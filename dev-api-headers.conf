# Dev/staging variant of api-headers.conf — adds X-Robots-Tag so search engines
# don't index non-production environments. nginx.conf uses this file in place of
# api-headers.conf at every location that overrides server-level headers.
include api-headers.conf;
add_header X-Robots-Tag 'noindex, nofollow, nosnippet, noarchive' always;