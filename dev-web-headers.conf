# Dev/staging variant of web-headers.conf — adds X-Robots-Tag so search engines
# don't index non-production environments. nginx.conf uses this file in place of
# web-headers.conf at every location that overrides server-level headers.
include web-headers.conf;
add_header X-Robots-Tag 'noindex, nofollow, nosnippet, noarchive' always;